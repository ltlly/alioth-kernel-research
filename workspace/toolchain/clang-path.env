# Sourced by scripts/build.sh
# Using system clang-21 (Ubuntu 1:21.1.8-6ubuntu1) — matches Android clang's
# major version 21, kernel was built with AOSP clang r563880c (LLVM 21).
# If module ABI / Android-specific clang flags become a problem, swap to
# AOSP prebuilt at workspace/toolchain/clang-aosp/clang-r584948 (already cloned).
CLANG_PATH="/usr/bin/clang"
CLANG_DIR="/usr/bin"
